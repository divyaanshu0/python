pipeline {
    agent any
    
    environment {
        REPO_URL = "https://github.com/your-repo/flask-app.git"  // Change this
        BRANCH = "main"
        IMAGE_NAME = "hello-world-app"
        DOCKER_HUB_USER = "your-dockerhub-username"  // Change this
    }

    stages {
        stage('Clone Repository') {
            steps {
                git branch: "${BRANCH}", url: "${REPO_URL}"
            }
        }

        stage('Build Docker Image') {
            steps {
                sh "docker build -t ${IMAGE_NAME} ."
            }
        }


        stage('Deploy on Server') {
            steps {
                sshagent(['server-ssh-credentials']) {
                    sh '''
                    docker stop flask_app || true
                    docker rm flask_app || true
                    docker run -d -p 5000:5000 --name flask_app {IMAGE_NAME}:latest
                    EOF
                    '''
                }
            }
        }
    }
}
